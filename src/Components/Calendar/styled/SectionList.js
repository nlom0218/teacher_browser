import styled from "styled-components";

const SectionList = styled.div`
  display: grid;
  row-gap: 10px;
  row-gap: 0.625rem;
  padding: 20px;
  padding: 1.25rem;
  font-size: 1.2em;
  font-size: 1.2rem;
`

export default SectionList