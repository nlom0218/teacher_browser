import styled from "styled-components";

const SectionNoDateText = styled.div`
  justify-self: center;
  color: ${props => props.theme.redColor};
  transition: color 1s ease;
  font-size: 1em;
  font-size: 1rem;
`

export default SectionNoDateText