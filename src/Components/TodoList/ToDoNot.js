import React from 'react';
import styled from 'styled-components';

const Container = styled.div`
    padding: 20px;
    padding: 1.25rem;
`

const ToDoNot = ({ notToDos }) => {
  console.log(notToDos);
  return (<Container>

  </Container>);
}

export default ToDoNot;